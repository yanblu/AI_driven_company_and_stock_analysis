# Model Experiments Archive

This directory preserves earlier model iterations, feature-selection studies, and alternative modelling approaches explored during development.

These experiments are retained for traceability but are not the recommended starting point. For the selected three-model XGBoost ensemble, current artifacts, evaluation notebooks, and documentation, see [`../final_model/`](../final_model/). For a concise modelling overview, return to the [Step 3 README](../README.md).
